---
import { withBase } from '../utils/with-base';

const currentPath = Astro.url.pathname;

// Helper to check if a link is active
const isActive = (path: string) => {
  const fullPath = withBase(path);
  return currentPath === fullPath || currentPath === `${fullPath}/`;
};
---

<header>
  <div class="header-content">
    <a href={withBase('/')} class="logo">Craftalog</a>

    <nav class="nav">
      <ul>
        <li>
          <a href={withBase('/')} class:list={[{ active: isActive('/') }]}>Home</a>
        </li>
        <li>
          <a href={withBase('/about')} class:list={[{ active: isActive('/about') }]}>About</a>
        </li>
      </ul>
    </nav>
  </div>
</header>

<style>
  header {
    border-bottom: var(--divider-border);
    box-shadow: var(--divider-shadow-bottom);
  }

  .header-content {
    max-width: 60rem;
    margin: 0 auto;
    padding: var(--space-6) var(--space-4);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
  }

  @media (min-width: 640px) {
    .header-content {
      padding-left: var(--space-8);
      padding-right: var(--space-8);
    }
  }

  .logo {
    font-family: var(--font-display);
    font-size: 1.375rem;
    font-weight: 700;
    letter-spacing: 0;
    color: var(--color-heading);
    border: none;
  }

  .logo:hover {
    border: none;
    opacity: 0.7;
  }

  .nav ul {
    display: flex;
    gap: var(--space-4);
    margin: 0;
  }

  .nav li {
    list-style: none;
  }

  .nav a {
    font-size: 0.9375rem;
    color: var(--color-muted);
    border: none;
    transition: color 0.2s;
  }

  .nav a:hover,
  .nav a.active {
    color: var(--color-fg);
    border: none;
  }

  .nav a:focus-visible {
    outline: 2px solid var(--color-fg);
    outline-offset: 2px;
  }

  .nav a.active {
    font-weight: 500;
  }
</style>
